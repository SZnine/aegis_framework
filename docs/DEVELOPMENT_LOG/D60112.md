不要总依赖AI迭代，有时候只是一句“请以后都这样简单回答”就解决所有问题了，对迭代而追求和AI思想行动高度同步是会让后期变的极为被动的，不过其好处也就是方便，更容易交流，但是缺点更严重。

将`../src/core`作为测试源代码根目录可以直接在整个项目中随时`import`。

先设定一个空字典作为请求头，其中的根本原因还没有确定。

`requests.exceptions.RequestException`可以抛出全部异常，而`requests.exceptions.HTTPError`只会抛出状态码异常。try中的异常会被此捕获，而不是优先被python异常处理。

